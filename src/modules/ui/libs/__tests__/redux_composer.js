const { describe, it } = global;
// import { expect } from 'chai';
// import sinon from 'sinon';

describe('manager.ui.libs.gen_podda_loader', function () {
  it('should map data properly');
  it('should handles errors thrown in the mapper');
  it('should pass props to the mapper');
  it('should pass env to the mapper');
  it('should unsubscribe to poddaStore when stopped');
});
