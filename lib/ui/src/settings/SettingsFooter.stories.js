import React from 'react';
import { storiesOf } from '@storybook/react';

import SettingsFooter from './SettingsFooter';

storiesOf('UI|Settings/SettingsFooter', module).add('default', () => <SettingsFooter />);
