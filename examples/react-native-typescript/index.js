import { AppRegistry } from 'react-native';
// eslint-disable-next-line import/extensions, import/no-unresolved
import App from './src/App';

AppRegistry.registerComponent('ReactNativeTypescript', () => App);
